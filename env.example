# MongoDB Connection
MONGODB_URI=your_mongodb_atlas_connection_string

# X (Twitter) OAuth 2.0 Configuration
X_CLIENT_ID=your_twitter_client_id
X_CLIENT_SECRET=your_twitter_client_secret

# Callback URL (fallback - the app will auto-detect based on request origin)
X_CALLBACK_URL=https://wen-gro-form-git-dev-yonis-projects-dee17595.vercel.app/auth/x/callback

# IMPORTANT: Configure your X app with BOTH callback URLs:
# 1. https://wen-gro-form-git-dev-yonis-projects-dee17595.vercel.app/auth/x/callback (for dev)
# 2. https://community.wengro.com/auth/x/callback (for production)
# The app will automatically use the correct one based on where the request comes from

# Session Configuration
SESSION_SECRET=your_session_secret_key_change_in_production

# Environment
NODE_ENV=production

# Frontend URL (optional, defaults to Vercel URL)
FRONTEND_URL=https://wen-gro-form-git-dev-yonis-projects-dee17595.vercel.app 